// Healing is currently handled inline by OverworldScene NPC interaction.
// This file exists as a placeholder for future extraction into a dedicated scene
// with a proper healing animation.
class HealScene extends Phaser.Scene {
  constructor() {
    super({ key: 'HealScene' });
  }
}
