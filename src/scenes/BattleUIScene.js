// BattleUIScene is currently integrated into BattleScene for MVP simplicity.
// This file exists as a placeholder for future separation of battle UI into
// a parallel overlay scene.
class BattleUIScene extends Phaser.Scene {
  constructor() {
    super({ key: 'BattleUIScene' });
  }
}
